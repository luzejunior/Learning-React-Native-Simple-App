const initialState = {
  page: 0,
  totalPages: 0,
  movies: [],
  selectedMovie: null
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case expression:

    default:
      return state;
  }
};

export default reducer;
